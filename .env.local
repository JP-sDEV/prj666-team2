# This file is used for CI/CD environments
# It contains mock values that won't actually connect to any real database

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=mock-secret-for-ci-cd-environment

# Google OAuth
GOOGLE_CLIENT_ID=mock-google-client-id
GOOGLE_CLIENT_SECRET=mock-google-client-secret

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prj666_db"

# MongoDB - Using a mock URI for CI/CD
MONGODB_URI=mongodb://mock-user:mock-password@localhost:27017/mock-db 
